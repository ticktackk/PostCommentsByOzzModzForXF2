{
    "template": "post_macros",
    "description": "Multi-quote option for comments",
    "execution_order": 10,
    "enabled": true,
    "action": "str_replace",
    "find": "<xf:if is=\"$xf.options.multiQuote\">",
    "replace": "<xf:if is=\"$xf.options.multiQuote && (!$post.thpostcomments_depth || ($post.thpostcomments_depth && $xf.options.thpostcomments_enableMultiQuote))\">"
}